{
  "general": {
    "level": "INFO",
    "workspace": "data/random",
    "path_samples": "samples",
    "path_models": "models",
    "seed": 0
  },
  "no_transfer_dqn": {
    "N": 100,
    "decay": 0.05,
    "dqn_params": {
      "gamma": 0.999,
      "batch_size_experience_replay": 128,
      "target_update": 10,
      "optimizer": "ADAM",
      "loss_function": "l2",
      "lr": 0.001,
      "weight_decay": 0.01,
      "workspace": "dqn"
    },
    "net_params": {
      "sizes": [
        4,
        16,
        8,
        2
      ]
    }
  },
  "transfer_dqn": {
    "N": 100,
    "decay": 0.05,
    "dqn_params": {
      "gamma": 0.999,
      "batch_size_experience_replay": 128,
      "target_update": 10,
      "optimizer": "ADAM",
      "loss_function": "l2",
      "lr": 0.001,
      "weight_decay": 0.01,
      "workspace": "dqn"
    },
    "net_params": {
      "sizes": [
        4,
        16,
        8,
        2
      ]
    }
  },
  "generate_samples": {
    "env_str": "CartPoleConfig-v0",
    "env_params": {
      "gravity": "np.random.choice(np.linspace(0.01,9.8+100,500),2)",
      "masscart": "np.random.choice(np.linspace(0.01,1.0+10,500),2)",
      "masspole": "np.random.choice(np.linspace(0.01,0.1+2,500),2)",
      "length": "np.random.choice(np.linspace(0.01,0.5+2,500),2)",
      "force_mag": "np.random.choice(np.linspace(0.01,10+100.0,500),2)"
    }
  },
  "learn_autoencoders": {
    "weight_decay": 0.0,
    "learning_rate": 0.001,
    "autoencoder_size": [
      8,
      32
    ],
    "optimizer_str": "ADAM",
    "normalize": true,
    "n_epochs": 5000,
    "criterion": "l1"
  },
  "N_base_trajectories": 10000
}